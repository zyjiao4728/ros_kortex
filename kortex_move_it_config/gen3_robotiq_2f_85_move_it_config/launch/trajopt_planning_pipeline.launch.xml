<launch>

  <!-- TrajOpt Plugin for MoveIt -->
  <arg name="planning_plugin" value="trajopt_interface/TrajOptPlanner" />
  <arg name="prefix" default="" />

  <!-- The request adapters (plugins) used when planning with TrajOpt. ORDER MATTERS! -->
  <arg name="planning_adapters" default="default_planner_request_adapters/AddTimeParameterization
                  default_planner_request_adapters/FixWorkspaceBounds
                  default_planner_request_adapters/FixStartStateBounds
                  default_planner_request_adapters/FixStartStateCollision
                  default_planner_request_adapters/FixStartStatePathConstraints" />

  <arg name="start_state_max_bounds_error" default="0.1" />

  <param name="planning_plugin" value="$(arg planning_plugin)" />
  <param name="request_adapters" value="$(arg planning_adapters)" />
  <param name="start_state_max_bounds_error" value="$(arg start_state_max_bounds_error)" />

  <rosparam command="load" file="$(find gen3_robotiq_2f_85_move_it_config)/config/trajopt_planning.yaml" />

</launch>